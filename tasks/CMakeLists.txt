# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(radar_baseTaskLib)
ADD_LIBRARY(${RADAR_BASE_TASKLIB_NAME} SHARED
    ${RADAR_BASE_TASKLIB_SOURCES})
add_dependencies(${RADAR_BASE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${RADAR_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${RADAR_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${RADAR_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${RADAR_BASE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${RADAR_BASE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${RADAR_BASE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${RADAR_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${RADAR_BASE_TASKLIB_HEADERS}
    DESTINATION include/orocos/radar_base)

